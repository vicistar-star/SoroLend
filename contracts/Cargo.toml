[workspace]
members = [
    "lending-pool",
    "collateral-manager",
    "stoken",
    "price-oracle",
    "flash-loan",
    "liquidation",
]
resolver = "2"

[profile.release]
opt-level = "z"
overflow-checks = true
lto = true
codegen-units = 1
